[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
addopts = --cov=vi_api_client --cov-report=xml --cov-report=term-missing
markers =
    integration: Offline integration tests using mock data.
